% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/batch-geocode-job.R
\name{geocode_file_job}
\alias{geocode_file_job}
\alias{geocode_file}
\title{Batch Geocode a File}
\usage{
geocode_file_job(
  path = NULL,
  field_mapping,
  ...,
  item_id = NULL,
  server_url = NULL,
  geocoder = default_geocoder(),
  token = arc_token()
)

geocode_file(
  path,
  field_mapping,
  out,
  ...,
  interval = 5,
  geocoder = default_geocoder(),
  token = arc_token()
)
}
\arguments{
\item{path}{String. Path to a CSV of addresses to upload and geocode.}

\item{field_mapping}{Named character vector mapping geocoder fields to
columns in \code{path}, for example \code{c(SingleLine = "full_address")}.
Case sensitive.}

\item{item_id}{String. Id of a CSV already in the portal. Use instead of \code{path}.}

\item{server_url}{String. Overrides the job URL derived from \code{geocoder}.}

\item{geocoder}{default \code{\link[=default_geocoder]{default_geocoder()}}.}

\item{token}{an object of class \code{httr2_token} as generated by \code{\link[arcgisutils:auth_code]{auth_code()}}
or related function}

\item{out}{String. Where to write the result CSV.}

\item{interval}{Number. Seconds between status checks.}
}
\value{
A \code{BatchGeocodeJob}.
}
\description{
Submits a file of addresses to the \code{BatchGeocode} job endpoint.
}
\examples{
\dontrun{
job <- geocode_file_job("addresses.csv", c(SingleLine = "full_address"))
job$await()
write_geocode_result(job, "geocoded.csv")
}
}
